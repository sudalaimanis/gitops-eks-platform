# yaml-language-server: $schema=
# Base values — GitHub Actions automatically updates `image.tag` on every push to main.
# ArgoCD watches this file and syncs the cluster when it changes.

replicaCount: 2

image:
  repository: 022499014268.dkr.ecr.us-east-1.amazonaws.com/sample-app
  tag: "34ec53892a21e3d8a7bb3363f586b3af08d05e84"
  pullPolicy: IfNotPresent

app:
  name: sample-app
  version: "1.0.0"
  environment: dev

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

ingress:
  enabled: true

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "250m"
    memory: "256Mi"

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 60

livenessProbe:
  path: /health
  initialDelaySeconds: 10
  periodSeconds: 15

readinessProbe:
  path: /ready
  initialDelaySeconds: 5
  periodSeconds: 10
